apiVersion: asgard.dev/v1
kind: Project
metadata:
  name: ingestion-pipeline
  namespace: data-platform
  title: Streaming Ingestion Pipeline
  description: >-
    Real-time ingestion pipeline that lands events from Kafka into the lakehouse
    and powers downstream analytics.
  labels:
    domain: data
    tier: gold
  annotations:
    asgard.dev/source-repo: https://github.com/example-org/ingestion-pipeline
  tags:
    - streaming
    - kafka
    - lakehouse
spec:
  id: proj-2024-0193
  owner: group:data-platform/ingestion-team
  manager: user:default/a.rivera
  classification: wide-operational
  budgetUsd: 250000
  lifecycle: active
relations:
  - type: dependsOn
    target: system:default/kafka-cluster
  - type: partOf
    target: domain:data-platform/analytics
