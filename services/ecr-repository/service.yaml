id: ecr-repository
name: ECR Repository
category: compute
status: live
classification_min: poc
classification_max: critical-path
description: Container image repository (scan-on-push).
auto_approvable: true
required_fields: [name]
provisioner:
  connector: terraform
  config:
    module: ./modules/aws/ecr-repository
    cloud: aws
cost:
  model: usage
  estimated_monthly_usd: 1.0
  source:
    type: aws-cost-explorer
tags: [compute, aws]
documentation: services/ecr-repository/README.md
